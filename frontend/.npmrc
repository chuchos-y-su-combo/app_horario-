# Allow electron and related packages to run their postinstall build scripts.
# In pnpm 11 this setting moved; listing it in both package.json["pnpm"] and here
# for compatibility across pnpm minor versions.
only-built-dependencies[]=@electron/get
only-built-dependencies[]=electron
only-built-dependencies[]=electron-builder
only-built-dependencies[]=esbuild
